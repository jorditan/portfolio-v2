---
import { ChevronDown } from "lucide-react";
import { ChevronUp } from "lucide-react";
const { tittle, text, periodo, empresa, destacado } = Astro.props;
---

<details
	class="group overflow-hidden w-full flex transition-all dark:hover:bg-black/18
        hover:bg-gray-100 border-gray-500 hover:bg-opacity-50 dark:border-white/30 flex-col gap-2 border-solid border-2 rounded-lg"
	onclick="handleClick()"
>
	<summary class="flex justify-between items-start sm:items-center px-3 pt-3">
		<div class="flex flex-col gap-2 sm:gap-0">
			<div class="flex gap-2 sm:gap-4 flex-col sm:flex-row">
				<span
					class="text-[12px] md:text-[18px] font-semibold text-slate-900 dark:text-slate-50"
					><span class="">{tittle}</span> - {empresa}</span
				>
			</div>
			<small class="text-slate-900 dark:text-slate-50 font-thin text-[12px]"
				>{periodo}</small
			>
		</div>
		<ChevronDown
			className="icon text-slate-900 dark:text-slate-50 group-open:hidden"
		/>
		<ChevronUp
			className="icon text-slate-900 dark:text-slate-50 hidden group-open:block"
		/>
	</summary>

	<div class="mb-3 mx-4">
		<hr class="h-px my-2 bg-slate-900 border-0 dark:bg-gray-500" />
		<p class="text-slate-900 mx-2 dark:text-gray-200 text-[14px]">
			{text}
			<span class="underline font-bold decoration-2 decoration-sky-500">
				{destacado}</span
			>.
		</p>
	</div>
</details>
